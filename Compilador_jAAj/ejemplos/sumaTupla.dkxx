declare{
	tupla[int, bool] = [3, true];
	int a = 3;
	bool b = true;
}
main{
	output(a);
	# Mostrar el contenido de la tupla
	output(tupla[0]);
	# output(tupla[1]);
	# Sumar con el contenido de la tupla
	output(5 + tupla[0]);
}